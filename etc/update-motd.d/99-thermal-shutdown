#!/bin/sh
# Show the note left by drive-temp-guard when it shut the machine down.
NOTE=/var/lib/sys-oled/thermal-shutdown.txt
[ -s "$NOTE" ] || exit 0
printf '\n*** This machine was powered off by drive-temp-guard ***\n'
cat "$NOTE"
printf 'Clear this notice with: sudo rm %s\n\n' "$NOTE"
